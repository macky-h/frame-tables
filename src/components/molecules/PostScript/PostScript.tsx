import React from 'react'
import './../../../assets/postscript.scss'
import { SectionTitle } from '../../atoms/SectionTitle'

export const PostScript = () => {
  return(
    <section className='ft-afterword'>
      <SectionTitle text='PostScript' />
    </section>
  )
}